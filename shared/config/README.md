# Shared Config

Reusable configuration examples belong here when they apply across stories and tools.

Guidelines:

- Do not store real local config, secrets, tokens, or active story paths.
- Keep defaults provider-neutral and local-model friendly.
- Put story-specific settings in the story workspace, not this tools repository.
